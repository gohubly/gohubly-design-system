import styled from "styled-components";
import { CommonProps } from "../types";
import { BaseRebassComponent } from "../base";

export const Flex = styled(BaseRebassComponent)<CommonProps>`
  display: flex;
`